<h1>Listing library_cards</h1>

<table>
  <tr>
    <th>User</th>
    <th>Number</th>
    <th>Zip code</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @library_cards.each do |library_card| %>
  <tr>
    <td><%= library_card.user_id %></td>
    <td><%= library_card.number %></td>
    <td><%= library_card.zip_code %></td>
    <td><%= link_to 'Show', library_card %></td>
    <td><%= link_to 'Edit', edit_library_card_path(library_card) %></td>
    <td><%= link_to 'Destroy', library_card, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Library card', new_library_card_path %>
